import React from 'react';
import Layout from '../components/common/Layout';

const Emails = () => {
  // In a real app, you would fetch this data
  const emails = [];

  return (
    <Layout>
      <h1>Emails</h1>
      {/* In a real app, you would have an EmailList component here */}
    </Layout>
  );
};

export default Emails;
