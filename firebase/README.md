# Firebase Setup

This directory contains the configuration and Cloud Functions for the Firebase project.

## Files

-   `firestore.rules`: Security rules for Firestore.
-   `storage.rules`: Security rules for Cloud Storage.
-   `firebase.json`: Configuration for Firebase Hosting and other services.
-   `functions/`: Directory containing Cloud Functions.
